# 점검 결과 — 구현 일치
점검 일시: 2026-04-10
점검 플랜: 2개 (weblist-master.md, phase4-api-mcp.md) / 구현 파일: 6개

---

## 갭 항목

| 번호 | 플랜 항목 | 플랜 상태 | 문제 내용 |
|------|---------|---------|---------|
| 1 | P4-1 ItemDetail UI — 복사 버튼 | ✅ 완료 처리 | 플랜 스펙에 복사 버튼 미포함. ItemRow.jsx DetailPanel의 `<pre>` 코드블록에 복사 기능 없음. 플랜과 구현 모두 누락 상태. |
| 2 | P4-5 Vercel MCP | ✅ 완료 처리 | `mcp: { info_url: "..." }` 형태로만 저장됨. desktop_config·cli_command 없어 UI에 코드블록 미표시. 신규 품질 기준(info_url 단독 사용 금지) 위반. |
| 3 | P4-5 Netlify MCP | ✅ 완료 처리 | 플랜 체크리스트에 "조사 필요"로 기재됐으나 mcp 필드 없이 완료 처리됨. 실제 MCP 존재 여부 미확인 상태. |
| 4 | P4-6 ChatGPT(OpenAI) mcp | ✅ 완료 처리 | `mcp: { info_url: "..." }` 단독 사용. OpenAI는 MCP 서버가 아닌 MCP 클라이언트이므로 mcp 필드 자체를 제거하고 notes에 언급하는 것이 올바른 처리. |

---

## 정상 확인 항목

- P4-1 UI: api_docs·dashboard·mcp 렌더링(구분선·링크·코드블록) 정상 ✅
- P4-2 mcp-automation: Pipedream·Apify MCP desktop_config+cli_command+info_url 모두 포함 ✅
- P4-2 mcp-automation: LangChain·LangGraph·CrewAI·AutoGen·Dify·n8n·Zapier·Make api_docs·dashboard 정상 ✅
- P4-3 dev-tools: Playwright·Sentry·Docker MCP 포함, 나머지 api_docs·dashboard 정상 ✅ (별도 확인)
- P4-4 database: PostgreSQL·SQLite·MongoDB·Neon·Redis MCP 포함 ✅ (별도 확인)
- P4-6 ai-platforms: Perplexity·Hugging Face·Ollama MCP desktop_config+cli_command+info_url 모두 포함 ✅

---

## 플랜 업데이트 필요

없음 (미완료 항목은 플랜에 🔲로 정상 표시됨)

---

## 수정 제안

| 번호 | 처리 방향 |
|------|---------|
| 1 | P4-1 플랜에 "복사 버튼" 항목 추가 후 ItemRow.jsx DetailPanel에 구현 |
| 2 | Vercel MCP 재조사 후 desktop_config+cli_command 추가 또는 mcp 필드 제거 |
| 3 | Netlify MCP 재조사 후 존재하면 추가, 없으면 필드 제거 |
| 4 | ChatGPT mcp 필드 제거, detail.notes에 "OpenAI API는 MCP 클라이언트로 동작" 언급 추가 |
