<!--
╔══════════════════════════════════════════════════════════════╗
║  도로명통합-코드.md  ·  한글 Python 내장 버전 (Code-bundled)    ║
╠══════════════════════════════════════════════════════════════╣
║                                                              ║
║  【버전 특징】                                                 ║
║    Python 코드 완전 내장 — 매번 동일한 코드 실행 보장            ║
║    결과가 일관적, 검증된 로직 그대로 재사용                      ║
║    프롬프트 전용 버전 → 도로명통합.md 참고                      ║
║                                                              ║
║  【설치 방법】                                                 ║
║    # 전역 등록 (모든 프로젝트에서 사용)                         ║
║    cp 도로명통합-코드.md ~/.claude/commands/                   ║
║                                                              ║
║    # 프로젝트 등록 (해당 프로젝트에서만 사용)                    ║
║    cp 도로명통합-코드.md ./.claude/commands/                   ║
║                                                              ║
║  【사용법】  /도로명통합-코드 [시도명] [폴더경로]                 ║
║                                                              ║
║  【예시】                                                     ║
║    /도로명통합-코드 충남 /Users/me/Documents/충남_도로명         ║
║    /도로명통합-코드 전북 C:\Users\me\Desktop\전북_도로명         ║
║    /도로명통합-코드 경기 ~/Downloads/경기_도로명                 ║
║                                                              ║
║  【출력 파일】  [폴더경로]/[시도명]_도로명정보조회.xlsx            ║
║                                                              ║
║  【사전 요구사항】  pip install pandas openpyxl                 ║
╚══════════════════════════════════════════════════════════════╝
-->
---
description: 시도별 도로명정보조회 xlsx 파일들을 하나의 통합 파일로 병합합니다 (Python 내장). 총건수 행·중복 헤더 자동 제거, 서식 포함 저장. 사용법: /도로명통합-코드 [시도명] [폴더경로]
allowed-tools: Bash, Read, Write, Edit, Glob
---

# 도로명정보조회 엑셀 통합 (한글 · Python 내장)

## 입력 파싱

`$ARGUMENTS`에서 시도명과 폴더경로를 추출합니다.

- 첫 번째 토큰 = 시도명 (예: 충남, 전북, 경기)
- 두 번째 토큰 = 폴더경로

둘 중 하나라도 없으면 사용자에게 확인 후 진행합니다.

## 처리 규칙

```
Row 0  →  "총N건 (도로명수 : N)"   ← 모든 파일에서 제거
Row 1  →  헤더 행                  ← 첫 번째 파일에서만 포함
Row 2+ →  실제 데이터              ← 모든 파일에서 포함
```

## 실행

아래 Python 코드를 **그대로** Bash 도구로 실행합니다.
`PROVINCE`와 `FOLDER` 변수만 사용자 입력값으로 교체합니다.

```python
import os, pandas as pd
from openpyxl import Workbook
from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
from openpyxl.utils import get_column_letter

# ── 설정 ──────────────────────────────────────────────────────
PROVINCE = "[시도명]"       # 예: 충남
FOLDER   = "[폴더경로]"     # 예: /Users/me/충남_도로명
# ─────────────────────────────────────────────────────────────

FOLDER   = os.path.abspath(FOLDER)
OUT_FILE = f"{PROVINCE}_도로명정보조회.xlsx"
OUT_PATH = os.path.join(FOLDER, OUT_FILE)

files = sorted([f for f in os.listdir(FOLDER)
                if f.endswith(".xlsx") and f != OUT_FILE])

if not files:
    raise FileNotFoundError(f"'{FOLDER}'에서 xlsx 파일을 찾을 수 없습니다.")

print(f"발견된 파일: {len(files)}개\n")
all_data, header = [], None

for i, fname in enumerate(files):
    df = pd.read_excel(os.path.join(FOLDER, fname), sheet_name=0, header=None)
    if i == 0:
        header = df.iloc[1].tolist()
    chunk = df.iloc[2:].reset_index(drop=True)
    all_data.append(chunk)
    print(f"  [{i+1:02d}] {fname}  →  {len(chunk):,}행")

combined = pd.concat(all_data, ignore_index=True)
print(f"\n통합 완료: {len(combined):,}행 × {len(header)}열")

# ── openpyxl 서식 저장 ────────────────────────────────────────
wb = Workbook()
ws = wb.active
ws.title = "도로명정보조회"

thin   = Side(style="thin")
border = Border(left=thin, right=thin, top=thin, bottom=thin)

H_FONT  = Font(name="Arial", bold=True, size=10, color="FFFFFF")
H_FILL  = PatternFill(fill_type="solid", start_color="4472C4", end_color="4472C4")
H_ALIGN = Alignment(horizontal="center", vertical="center", wrap_text=True)
D_FONT  = Font(name="Arial", size=10)
D_ALIGN = Alignment(vertical="center")

for c, name in enumerate(header, 1):
    cell = ws.cell(row=1, column=c, value=name)
    cell.font = H_FONT; cell.fill = H_FILL
    cell.alignment = H_ALIGN; cell.border = border

for r, row in enumerate(combined.itertuples(index=False), 2):
    for c, val in enumerate(row, 1):
        cell = ws.cell(row=r, column=c)
        if pd.isna(val):
            cell.value = None
        elif isinstance(val, float) and val == int(val):
            cell.value = int(val)
        else:
            cell.value = val
        cell.font = D_FONT; cell.alignment = D_ALIGN; cell.border = border

COL_WIDTHS = [8, 12, 20, 30, 10, 8, 14, 8, 8, 10]
for i in range(len(header)):
    ws.column_dimensions[get_column_letter(i+1)].width = (
        COL_WIDTHS[i] if i < len(COL_WIDTHS) else 15)

ws.row_dimensions[1].height = 20
ws.freeze_panes = "A2"
wb.save(OUT_PATH)
print(f"\n✅ 저장 완료: {OUT_PATH}")
```

## 완료 후 보고

- 생성된 파일 경로 안내
- 총 데이터 행 수 및 시군구별 건수 요약 (선택)
- 오류 발생 시 원인과 해결 방법 설명
