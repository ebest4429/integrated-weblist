<!--
╔══════════════════════════════════════════════════════════════╗
║  도로명편집병합-코드.md  ·  한글 Python 내장 버전              ║
╠══════════════════════════════════════════════════════════════╣
║                                                              ║
║  【버전 특징】                                                 ║
║    Python 코드 완전 내장 — 매번 동일한 코드 실행 보장            ║
║    1단계: 각 파일별 읍면동구분=2 행 제거 + 로마자표기 컬럼 제거   ║
║    2단계: 편집된 17개 파일 전체 병합 → 전국_도로명정보조회.xlsx   ║
║                                                              ║
║  【설치 방법】                                                 ║
║    # 전역 등록 (모든 프로젝트에서 사용)                         ║
║    cp 도로명편집병합-코드.md ~/.claude/commands/               ║
║                                                              ║
║    # 프로젝트 등록 (해당 프로젝트에서만 사용)                    ║
║    cp 도로명편집병합-코드.md ./.claude/commands/               ║
║                                                              ║
║  【사용법】  /도로명편집병합-코드 [폴더경로]                      ║
║                                                              ║
║  【예시】                                                     ║
║    /도로명편집병합-코드 C:\Users\me\2차편집                     ║
║    /도로명편집병합-코드 ~/Downloads/도로명_편집                  ║
║                                                              ║
║  【출력 파일】  [폴더경로]/전국_도로명정보조회.xlsx               ║
║                                                              ║
║  【사전 요구사항】  pip install pandas openpyxl                 ║
╚══════════════════════════════════════════════════════════════╝
-->
---
description: 시도별 도로명정보조회 xlsx 파일에서 읍면동구분=2 행 제거 및 로마자표기 컬럼 제거 후 전체 파일을 하나로 병합합니다. 사용법: /도로명편집병합-코드 [폴더경로]
allowed-tools: Bash, Read, Write, Edit, Glob
---

# 도로명정보조회 편집 및 전국 병합 (한글 · Python 내장)

## 입력 파싱

`$ARGUMENTS`에서 폴더경로를 추출합니다.

- 첫 번째 토큰 = 폴더경로 (예: C:\Users\me\2차편집)

폴더경로가 없으면 사용자에게 확인 후 진행합니다.

## 처리 규칙

각 파일에 적용하는 편집 규칙:

```
읍면동구분 == 2  →  해당 행 전체 제거
로마자표기 컬럼  →  컬럼 전체 제거
```

편집 완료 후 17개 파일을 하나로 병합합니다.

## 실행

아래 Python 코드를 **그대로** Bash 도구로 실행합니다.
`FOLDER` 변수만 사용자 입력값으로 교체합니다.

```python
import os, sys, pandas as pd
from openpyxl import Workbook
from openpyxl.styles import Font, PatternFill, Alignment, Border, Side
from openpyxl.utils import get_column_letter

# ── 설정 ──────────────────────────────────────────────────────
FOLDER   = "[폴더경로]"     # 예: C:\Users\me\2차편집
# ─────────────────────────────────────────────────────────────

FOLDER   = os.path.abspath(FOLDER)
OUT_FILE = "전국_도로명정보조회.xlsx"
OUT_PATH = os.path.join(FOLDER, OUT_FILE)

COL_DROP   = "로마자표기"
COL_FILTER = "읍면동구분"
FILTER_VAL = 2

files = sorted([f for f in os.listdir(FOLDER)
                if f.endswith(".xlsx") and f != OUT_FILE])

if not files:
    raise FileNotFoundError(f"'{FOLDER}'에서 xlsx 파일을 찾을 수 없습니다.")

print(f"대상 파일: {len(files)}개\n")
print("=" * 60)
print("1단계: 파일별 편집 (읍면동구분=2 제거 + 로마자표기 컬럼 제거)")
print("=" * 60)

all_data = []
header   = None

for i, fname in enumerate(files):
    fpath = os.path.join(FOLDER, fname)
    df    = pd.read_excel(fpath, sheet_name=0, header=0)

    before = len(df)

    # 읍면동구분 == 2 행 제거
    if COL_FILTER in df.columns:
        df = df[df[COL_FILTER] != FILTER_VAL].reset_index(drop=True)
    removed = before - len(df)

    # 로마자표기 컬럼 제거
    if COL_DROP in df.columns:
        df = df.drop(columns=[COL_DROP])

    print(f"  [{i+1:02d}] {fname}")
    print(f"        원본 {before:,}행  →  편집후 {len(df):,}행  (제거 {removed:,}행)")

    # 편집된 내용 저장 (원본 덮어쓰기)
    with pd.ExcelWriter(fpath, engine="openpyxl") as writer:
        df.to_excel(writer, index=False, sheet_name="도로명정보조회")

    if header is None:
        header = df.columns.tolist()
    all_data.append(df)

print()
print("=" * 60)
print("2단계: 전국 병합")
print("=" * 60)

combined = pd.concat(all_data, ignore_index=True)
print(f"병합 완료: {len(combined):,}행 x {len(header)}열\n")

# ── openpyxl 서식 저장 ────────────────────────────────────────
wb = Workbook()
ws = wb.active
ws.title = "도로명정보조회"

thin   = Side(style="thin")
border = Border(left=thin, right=thin, top=thin, bottom=thin)

H_FONT  = Font(name="Arial", bold=True, size=10, color="FFFFFF")
H_FILL  = PatternFill(fill_type="solid", start_color="4472C4", end_color="4472C4")
H_ALIGN = Alignment(horizontal="center", vertical="center", wrap_text=True)
D_FONT  = Font(name="Arial", size=10)
D_ALIGN = Alignment(vertical="center")

for c, name in enumerate(header, 1):
    cell = ws.cell(row=1, column=c, value=name)
    cell.font = H_FONT; cell.fill = H_FILL
    cell.alignment = H_ALIGN; cell.border = border

for r, row in enumerate(combined.itertuples(index=False), 2):
    for c, val in enumerate(row, 1):
        cell = ws.cell(row=r, column=c)
        if pd.isna(val):
            cell.value = None
        elif isinstance(val, float) and val == int(val):
            cell.value = int(val)
        else:
            cell.value = val
        cell.font = D_FONT; cell.alignment = D_ALIGN; cell.border = border

COL_WIDTHS = [8, 12, 20, 12, 10, 12, 10, 12, 15]
for i in range(len(header)):
    ws.column_dimensions[get_column_letter(i+1)].width = (
        COL_WIDTHS[i] if i < len(COL_WIDTHS) else 15)

ws.row_dimensions[1].height = 20
ws.freeze_panes = "A2"
wb.save(OUT_PATH)
print(f"저장 완료: {OUT_PATH}")
```

## 완료 후 보고

- 파일별 편집 결과 (원본 행수 / 편집후 행수 / 제거 행수)
- 전국 병합 총 행수
- 출력 파일 경로
- 오류 발생 시 원인과 해결 방법 설명
